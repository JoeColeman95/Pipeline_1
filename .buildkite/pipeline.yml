steps:
  - label: "Test docker"
    plugins:
      docker#v5.12.0:
        image: "alpine:latest"
        propagate-environment: true
        mount-buildkite-agent: true
        mount-ssh-agent: true
        environment:
          - BUILDKITE_AGENT_AWS_KMS_KEY
    command:
      - echo "BUILDKITE_AGENT_AWS_KMS_KEY = $BUILDKITE_AGENT_AWS_KMS_KEY"
      - ls -la /usr/bin/buildkite-agent
      - chmod +x /usr/bin/buildkite-agent
      - /usr/bin/buildkite-agent pipeline upload